@model ErrorViewModel
@{
    ViewData["Title"] = "InputError";
}

<h1 class="text-danger">RedirectError.</h1>
<h2 class="text-danger">RedirectError</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p>
}
